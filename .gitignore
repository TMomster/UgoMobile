# UGo Mobile —— 根目录 gitignore
# （android/ 内有 Capacitor 生成的独立 .gitignore，覆盖原生工程与打包产物）

# 依赖
node_modules/

# Rust / WASM 构建产物
core-go/target/
# 说明：www/wasm/ugo_core.wasm 是构建产物，但 App 运行必需，
# 有意纳入版本控制（更新方式见 README「开发与构建」）。

# 日志
*.log
npm-debug.log*

# 环境变量与本地配置
.env
.env.*

# IDE / 编辑器
.idea/
.vscode/
*.iml
*.swp

# 操作系统
.DS_Store
Thumbs.db
desktop.ini

# 临时与备份文件
*.tmp
*~
start_install.bat
